services:
  sentinel2:
    image: ${SENTINEL2_IMAGE:-sentinel2:local}
    build:
      context: .
      args:
        BUILD_VERSION: ${BUILD_VERSION:-}
    ports:
      - "8090:8090"
    env_file:
      - .env
    volumes:
      - pb_data:/app/pb_data

volumes:
  pb_data:
